---
schema_version: 2
id: analysis-transaction-ordering-guarantees
kind: analysis
title: 트랜잭션 시스템은 어떤 순서 보장을 제공하는가
aliases:
  - Transaction ordering guarantees
  - serializability linearizability external consistency
  - 트랜잭션 순서 보장
summary: serializability·snapshot isolation·linearizability·external consistency를 실행 단위와 시간 순서로 비교해, 강한 일관성이라는 표현이 숨기는 재시도와 조정 비용을 분석한다.
domains:
  - databases
  - concurrency
  - distributed-systems
editorial_status: draft
publication_visibility: unlisted
graph_visibility: context
created: 2026-08-05
updated: 2026-08-05
review:
  mode: pending
  revision: sha256:4d69dbece0ba0636c8b6186c1c43d2d7deac6e4b310028ebf0d11877cbce34cc
  reviewed_at: null
  reviewed_by: null
evidence_ids:
  - ref-111
  - ref-112
  - ref-113
  - ref-095
  - ref-089
capability_layers:
  - reliable-results
  - scalability
history:
  layer: system
---

## 문제 제기

“강한 일관성”이라는 표현만으로는 두 클라이언트의 객체 연산이 실제 시간 순서를 지키는지, 여러 트랜잭션의 결과가 어떤 직렬 실행과 같은지, 완료된 트랜잭션의 순서가 전역 시계와 맞는지를 알 수 없다. 이 분석은 네 보장 계층을 같은 축에 놓되 서로 다른 질문으로 분리한다.

## 네 가지 순서 계약

| 계약 | 주된 단위 | 확인하는 질문 | 대표 경계 |
|---|---|---|---|
| snapshot isolation | 트랜잭션 스냅샷 | 이 트랜잭션은 어느 버전의 상태를 읽는가 | write skew가 가능할 수 있음 |
| serializability | 트랜잭션 이력 | 성공한 커밋이 어떤 순차 실행과 동등한가 | anomaly 감지·중단·재시도 필요 |
| linearizability | 객체 연산 | 각 연산의 효과 시점과 real-time order는 무엇인가 | 여러 객체 트랜잭션의 전체 순서를 자동 보장하지 않음 |
| external consistency | 전역 트랜잭션 순서 | 완료 순서와 이후 관측 순서를 어떻게 맞추는가 | timestamp uncertainty·commit wait·조정 비용 |

이 표의 계층은 서로 대체되지 않는다. Snapshot Isolation이 안정된 읽기를 제공해도 serializable하지 않을 수 있고, 선형화 가능한 복제 객체가 있다고 해서 임의의 다중 객체 트랜잭션이 외부 일관성을 얻는 것도 아니다.

## Write skew가 드러내는 차이

의사가 한 명 이상 당직이어야 한다는 불변식이 있다고 하자. 두 트랜잭션이 서로 다른 의사를 `on duty`에서 `reserve`로 바꾸면서 각자의 snapshot에서 다른 의사가 아직 당직이라고 읽으면, 두 트랜잭션은 각각의 로컬 검사에는 성공한다. 그러나 함께 커밋한 결과에는 당직 의사가 없다.

이 이력은 “읽기가 안정적이었다”는 조건과 “전체 커밋 결과가 직렬 실행과 동등하다”는 조건이 다름을 보여준다. SSI는 이런 read/write dependency를 감지해 한쪽을 serialization failure로 되돌릴 수 있지만, 그 실패를 처음부터 다시 실행할 애플리케이션 정책이 필요하다.

## 시간 순서와 객체 순서

선형화 가능성은 큐·레지스터 같은 객체 연산의 외부 이력을 순차 명세로 설명한다. 직렬 가능성은 여러 연산을 묶은 트랜잭션의 읽기·쓰기 의존성을 본다. 외부 일관성은 여기에 완료 시각과 이후 시작된 작업의 관측 순서를 연결한다.

따라서 다음 비교를 문서에 남겨야 한다.

- 객체 하나의 `put`이 실제 시간 순서대로 보이는가.
- 트랜잭션 안에서 읽은 여러 객체의 조합이 어떤 직렬 순서로 설명되는가.
- 두 데이터센터에서 커밋된 트랜잭션의 timestamp와 사용자 관측 순서가 어떻게 정렬되는가.
- 충돌을 대기시키는지, 실패시키는지, 재시도시키는지.

## 선택 기준

정확성 계약은 성능 수치보다 먼저 고정해야 한다. 단일 객체의 최신값이 필요하면 선형화 가능한 저장소 연산이 충분할 수 있고, 업무 불변식을 여러 행에 걸쳐 유지해야 하면 serializable transaction과 재시도 경로가 필요하다. 지역 간 완료 순서까지 사용자에게 보장해야 하면 external consistency의 시계·복제·commit-wait 비용을 별도로 측정해야 한다.

그러므로 “strong consistency가 더 느리다”는 문장은 불완전하다. 어떤 순서 단위와 실패 처리까지 포함하는지에 따라 비용의 원인이 달라진다.

## 관계

| 관계 | 대상 | 설명 | 근거 |
|---|---|---|---|
| synthesizes | [[직렬 가능성]] | SI·write skew·SSI를 트랜잭션 이력의 순서 보장 문제로 종합한다. | [[Serializable Isolation for Snapshot Databases]], [[PostgreSQL 17 Transaction Isolation]] |
| constrains | [[선형화 가능성]] | 객체 연산의 real-time order와 트랜잭션의 serial equivalence를 비교한다. | [[Linearizability: A Correctness Condition for Concurrent Objects]] |
| synthesizes | [[외부 일관성과 시간 불확실성]] | 트랜잭션 순서와 완료 시각·commit wait를 하나의 전역 서비스 질문으로 연결한다. | [[Spanner - Google's Globally-Distributed Database]] |
| constrains | [[분산 서비스는 빠른 응답과 같은 상태를 어떻게 함께 보장하는가]] | 응답 시간과 상태 순서를 비교할 때 보장 단위·재시도·조정 비용을 함께 기록하게 한다. | [[PostgreSQL 17 Transaction Isolation]], [[Spanner - Google's Globally-Distributed Database]] |

## 출처

<!-- wiki-v2:evidence-start -->
### 근거 ID
- `ref-111`
- `ref-112`
- `ref-113`
- `ref-095`
- `ref-089`
<!-- wiki-v2:evidence-end -->

- [[A Critique of ANSI SQL Isolation Levels]]
- [[Serializable Isolation for Snapshot Databases]]
- [[PostgreSQL 17 Transaction Isolation]]
- [[Linearizability: A Correctness Condition for Concurrent Objects]]
- [[Spanner - Google's Globally-Distributed Database]]

## 관련 항목

- [[직렬 가능성]] — transaction history의 serial equivalence를 정의한다.
- [[선형화 가능성]] — concurrent object의 효과 시점과 실제 시간 순서를 설명한다.
- [[외부 일관성과 시간 불확실성]] — 전역 트랜잭션 순서와 시계 불확실성을 연결한다.
- [[분산 서비스는 빠른 응답과 같은 상태를 어떻게 함께 보장하는가]] — 서비스 응답과 상태 계약의 종단 비용을 분석한다.
- [[동시성]] — 순서 보장 문제의 일반적인 실행 배경을 제공한다.
