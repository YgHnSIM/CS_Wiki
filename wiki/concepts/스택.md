---
title: 스택
aliases: [Stack, 호출 스택, call stack]
summary: "복귀 주소와 실행 상태를 LIFO 순서로 저장해 중첩 호출과 재귀를 가능하게 하는 구조."
tags: [type/concept, domain/software-engineering, domain/computer-architecture, status/active]
created: 2026-05-04
updated: 2026-07-22
sources: ["서브루틴과 스택(Stack)의 원리.md", "Structured Programming"]
status: active
graph_id: concept-c297dc5b421f48a3
---

## 개요

[[스택]]은 나중에 넣은 값을 먼저 꺼내는 LIFO(Last In, First Out) 구조다. [[서브루틴]] 호출에서는 가장 나중에 호출된 함수가 가장 먼저 반환되므로, [[복귀 주소]]를 스택에 저장하면 중첩 호출과 재귀 호출을 자연스럽게 처리할 수 있다.

## 호출 구조에서의 역할

현대적 호출 구조에서 `CALL`은 보통 호출 지점 다음 명령의 주소를 스택에 저장하고, 프로그램 카운터를 서브루틴 시작 주소로 바꾼다. `RET`은 스택에서 복귀 주소를 꺼내 그 위치로 실행을 되돌린다.

```text
CALL -> PUSH return_address
RET  -> POP return_address
```

이 구조는 [[Wheeler Jump]]처럼 서브루틴 내부의 고정된 위치에 복귀 점프를 덮어쓰는 방식보다 중첩 호출과 재귀에 강하다.

[[Structured Programming]]의 관점에서 스택은 절차 호출뿐 아니라 중첩된 반복 구조의 진행 상태를 추적하는 데도 연결된다. 반복 구문에 들어갈 때마다 현재 반복 횟수 같은 동적 지표가 생기고, 중첩된 반복에서는 이 지표들이 LIFO 구조로 쌓인다. 이 점에서 스택은 [[제어 흐름]]을 사람이 설명 가능한 구조로 유지하는 실행 상태 장치이기도 하다.

## 확장

스택은 복귀 주소만 저장하는 장치에서 [[스택 프레임]]을 통해 함수 실행 환경 전체를 담는 구조로 확장되었다. 매개변수, 지역 변수, 저장해야 할 레지스터, 이전 프레임 포인터 등이 호출마다 별도 영역에 배치된다.

스택은 실행 흐름을 기억하기 때문에 보안상 중요한 공격 표면이기도 하다. 스택 버퍼 오버플로우는 복귀 주소를 덮어써 실행 흐름을 바꿀 수 있으며, 현대 시스템은 stack canary, NX bit, ASLR, shadow stack 같은 방어 기법을 사용한다.

## 출처

- [[서브루틴과 스택(Stack)의 원리]]
- [[Structured Programming]]

## 관련 항목

- [[복귀 주소]] — 서브루틴이나 함수가 끝난 뒤 실행을 재개해야 할 위치를 가리키는 주소.
- [[스택 프레임]] — 함수 호출마다 스택에 마련되는 매개변수, 복귀 주소, 지역 변수 등의 실행 환경.
- [[서브루틴]] — 호출 후 원래 위치로 돌아오는 재사용 가능한 코드 단위.
- [[Wheeler Jump]] — 호출부 명령을 이용해 복귀 점프를 만들어내는 EDSAC의 초기 서브루틴 호출 기법.
- [[소프트웨어 공학]] — 소프트웨어를 신뢰 가능하고 이해 가능하며 유지보수 가능한 구조로 만드는 공학적 실천.
