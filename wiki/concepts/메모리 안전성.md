---
title: 메모리 안전성
aliases: [Memory Safety, 메모리 보안, memory safety]
summary: "프로그램이 할당된 메모리 범위와 생명주기 안에서 데이터를 다루도록 보장하는 성질."
tags: [type/concept, domain/software-engineering, domain/security, status/active]
created: 2026-05-12
updated: 2026-07-22
sources: ["데이터 표현과 인코딩.md", "The Development of the C Language"]
status: active
graph_id: concept-0939ab2408573c58
---

## 개요

[[메모리 안전성]]은 프로그램이 할당된 메모리 범위와 생명주기 안에서만 데이터를 읽고 쓰도록 보장하는 성질이다. 이 소스는 C 언어의 초기화되지 않은 메모리, [[C 문자열]]의 널 종단 누락, [[정수 오버플로]]가 메모리 안전성 문제로 이어질 수 있음을 설명한다.

## 주요 위험

- 초기화되지 않은 메모리는 이전 데이터가 남아 있어 예측 불가능한 동작이나 정보 유출을 만들 수 있다.
- 널 종단이 빠진 C 문자열은 버퍼 경계를 넘어 읽히며 버퍼 오버리드와 정보 유출을 유발할 수 있다.
- 정수 오버플로는 메모리 할당 크기나 인덱스 계산을 잘못 만들어 경계 밖 접근으로 이어질 수 있다.

## 설계 원칙

메모리를 다룰 때는 값의 [[데이터 표현]], 버퍼 크기, 타입 범위, 생명주기, 경계 검사를 함께 고려해야 한다. 고수준 추상화를 사용하더라도 실제 데이터가 어떤 바이트 패턴으로 저장되고 어디에서 경계를 넘을 수 있는지 이해하는 것이 안정적인 시스템 설계의 기초가 된다.

[[C 언어]]는 [[시스템 프로그래밍]]에 필요한 포인터와 바이트 단위 제어를 제공하지만, 그만큼 경계와 생명주기 검사를 언어가 자동으로 강제하지 않는다. Unix와 C의 결합은 생산성과 이식성을 높였지만, 이후 시스템 소프트웨어에서 메모리 안전성이 별도의 공학 문제로 커지는 배경도 만들었다.

## 출처

- [[데이터 표현과 인코딩]]
- [[The Development of the C Language]]

## 관련 항목

- [[C 문자열]] — Null terminator로 문자열 경계를 표시하는 C 스타일 문자열 표현.
- [[C 언어]] — Unix 구현을 위해 발전한 시스템 프로그래밍 언어로, 포인터와 바이트 단위 제어를 고급 언어 구조와 결합한 언어.
- [[시스템 프로그래밍]] — 운영체제, 컴파일러, 셸, 장치 제어처럼 기반 소프트웨어를 작성하는 프로그래밍 영역.
- [[정수 오버플로]] — 정수 연산 결과가 타입의 표현 범위를 넘어 안정성과 보안 문제를 일으키는 현상.
- [[비트와 바이트]] — 비트와 8비트 바이트를 중심으로 데이터 표현의 최소 단위와 해석 범위를 정리한 개념.
