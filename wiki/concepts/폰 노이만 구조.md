---
schema_version: 2
id: concept-7d1c4861fc9f4cd5
kind: concept
title: 폰 노이만 구조
aliases:
  - Von Neumann Architecture
  - von Neumann model
  - Princeton architecture
summary: 명령과 데이터를 같은 메모리 체계에 두고 제어 장치가 명령을 가져와 실행하는 컴퓨터 구조.
domains:
  - computer-architecture
  - computer-history
editorial_status: active
publication_visibility: public
graph_visibility: public
created: 2026-07-10
updated: 2026-07-26
review:
  mode: legacy-baseline
  revision: sha256:5acdeec4616473a21953cd6ee631e0456086ebc4d6cd205ab988a149b15993da
  reviewed_at: null
  reviewed_by: legacy-baseline
evidence_ids:
  - ref-006
capability_layers: []
redirect_from:
  - /concepts/폰-노이만-구조/
---

## 개요

[[폰 노이만 구조]]는 명령과 데이터를 같은 메모리 체계에 저장하고, 제어 장치가 메모리에서 명령을 가져와 순차적으로 실행하는 컴퓨터 구조를 가리키는 이름이다. 이 이름은 [[존 폰 노이만]]의 [[First Draft of a Report on the EDVAC]]가 널리 유통되면서 굳어졌다.

## 구성 요소

폰 노이만 구조의 전형적 설명은 다음 요소를 포함한다.

- 산술 장치: 수치 연산을 수행한다.
- 제어 장치: 명령을 해석하고 실행 순서를 제어한다.
- 메모리: 데이터와 명령을 저장한다.
- 입력 장치: 외부 정보를 기계 내부로 가져온다.
- 출력 장치: 계산 결과를 외부로 내보낸다.
- 외부 기록 매체: 느리지만 더 큰 저장 공간이나 입출력 매체로 기능한다.

이 구성은 현대 컴퓨터 구조 설명의 기본 어휘가 되었지만, 당시에는 회로와 배선을 재구성하던 계산 장치에서 저장되고 조작되는 프로그램으로 넘어가는 개념적 전환이었다.

## 실행을 움직이는 세 축

현대적 [[하드웨어 실행 모델]] 관점에서 폰 노이만 구조는 연산과 저장의 2축에 머무르지 않고 **연산·저장·제어**의 3축 체계로 구체화된다.

| 축 | 하드웨어 담당 | 역할 | 직조에서의 비유 | 실행 모델에서의 위상 |
|---|---|---|---|---|
| **연산** | ALU + 범용 레지스터 | 값을 변경한다 | **씨실** (변화의 실체) | 계산의 주체 (ALU는 레지스터만 직접 계산) |
| **저장** | RAM + 시스템 버스 | 칸을 기억한다 | **날실** (상태의 바탕) | 데이터의 거처 (바이트 주소 공간) |
| **제어** | 프로그램 카운터 (PC / RIP) | 다음 실행할 명령을 선택한다 | **바늘 / 북** (흐름의 지휘) | Fetch의 출발점 (제어 흐름 지휘) |

프로그래밍은 제어라는 바늘이 날실(저장)과 씨실(연산) 사이를 오가며 명령 인출과 데이터 처리를 직조하는 과정이다. ALU는 RAM을 직접 다루지 못하고 레지스터에 적재된 값만 계산하며, 제어 장치는 PC 레지스터를 통해 명령 인출(Fetch)의 흐름을 지휘한다.

## 저장 프로그램 방식과의 관계

폰 노이만 구조의 핵심은 [[저장 프로그램 컴퓨터]]의 원리와 겹친다. 프로그램 명령이 데이터와 같은 메모리에 놓이면, 프로그램은 다른 프로그램에 의해 적재되거나 수정될 수 있다. 이 성질 때문에 [[로더]], [[어셈블러]], [[링커]], 컴파일러, 운영체제 같은 소프트웨어 계층이 가능해진다.

동시에 이 구조는 병목도 만든다. 명령과 데이터가 같은 기억 장치와 통로를 공유하면, 명령을 가져오는 일과 데이터를 읽고 쓰는 일이 같은 자원을 두고 경쟁할 수 있다. 이 문제는 훗날 폰 노이만 병목이라는 이름으로 설명된다.

## 명칭의 한계

폰 노이만 구조라는 이름은 편리하지만, 저장 프로그램 개념의 형성을 한 사람에게만 돌리기 쉽다는 한계가 있다. EDVAC 설계 논의는 Moore School의 ENIAC/EDVAC 팀과 군사 연구 환경, 전자식 메모리 기술, 수학적 논리화가 결합된 결과였다. 따라서 이 위키에서는 명칭은 사용하되, [[저장 프로그램 개념의 여러 기원]] 안에서 집단적·계보적 맥락을 함께 기록한다.

## 출처

<!-- wiki-v2:evidence-start -->
### 근거 ID
- `ref-006`
<!-- wiki-v2:evidence-end -->

- [[First Draft of a Report on the EDVAC]]

## 관련 항목

- [[저장 프로그램 컴퓨터]] — 프로그램 명령을 데이터처럼 메모리에 저장하고 실행하는 컴퓨터 구조.
- [[하드웨어 실행 모델]] — 연산(ALU)·저장(RAM)·제어(PC)의 3축 분리와 버스 분업 위에서 명령 사이클을 회전시키는 물리적 컴퓨터 동작 구조.
- [[EDVAC]] — 저장 프로그램 컴퓨터의 논리 구조를 문서화하고 후속 기계 설계에 영향을 준 초기 전자식 컴퓨터 설계.
- [[존 폰 노이만]] — EDVAC 보고서를 통해 저장 프로그램 컴퓨터의 논리 구조와 폰 노이만 구조 명칭이 확산되는 데 핵심 역할을 한 수학자.
- [[Initial Orders]] — EDSAC에서 종이 테이프 프로그램을 읽고 해석해 메모리에 적재한 초기 로더·어셈블러 절차.
