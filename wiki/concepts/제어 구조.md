---
schema_version: 2
id: concept-7cc49c1806726804
kind: concept
title: 제어 구조
aliases:
  - Control structure
  - structured control
  - 제어문
summary: 실행 흐름을 순차·선택·반복·절차 호출 같은 사람이 추적 가능한 형태로 제한하는 언어 장치.
domains:
  - programming-languages
  - software-engineering
editorial_status: active
publication_visibility: public
graph_visibility: public
created: 2026-07-10
updated: 2026-07-26
review:
  mode: legacy-baseline
  revision: sha256:4147286cbb2aa2a18a3319f7da325c62b0d1c692861e1c17c2401716c56ac9da
  reviewed_at: null
  reviewed_by: legacy-baseline
evidence_ids:
  - ref-011
  - ref-013
  - ref-014
capability_layers: []
redirect_from:
  - /concepts/제어-구조/
---

## 개요

[[제어 구조]]는 프로그램의 [[제어 흐름]]을 일정한 형태로 제한하고 명명하는 언어 장치다. 순차 실행, 조건 선택, 반복, 절차 호출, `case`, 오류 탈출 구문은 모두 실행 흐름을 임의 점프가 아니라 식별 가능한 구조로 표현하게 한다.

## 구조의 역할

제어 구조의 목적은 계산 능력을 늘리는 데만 있지 않다. 이미 [[구조화 프로그램 정리]]가 보여주듯, 제한된 구조만으로도 많은 계산 절차를 표현할 수 있다. 중요한 효과는 프로그램 텍스트와 실행 과정 사이의 대응을 단순하게 유지해, 사람이 프로그램을 읽고 검증하고 수정할 수 있게 만드는 데 있다.

## goto와의 관계

[[GOTO 문]]은 제어 흐름을 직접 이동시키는 원시적 장치다. 구조적 프로그래밍은 `goto`가 필요했던 반복, 선택, 오류 탈출, 다중 분기 같은 패턴을 더 명확한 제어 구조로 흡수하려는 흐름으로 볼 수 있다.

## 하드웨어적 실체와 PC 레지스터

고급 프로그래밍 언어의 `if`, `while`, `return` 같은 제어문은 하드웨어에 "조건"이라는 별도의 전용 회로가 존재해서 동작하는 것이 아니다.

[[하드웨어 실행 모델]] 수준에서 모든 조건 분기는 **프로그램 카운터(PC) 레지스터에 적힌 주소 값을 바꿀 것인가(Branch/Jump) 순차적으로 유지할 것인가**의 물리적 선택으로 환원된다. ALU 연산 결과 플래그에 따라 PC 값이 갱신되거나 다음 명령 주소로 전진하는 하드웨어 메커니즘을, 사람이 추적하고 검증하기 쉬운 구문 블록으로 묶어낸 것이 제어 구조다.

## 출처

<!-- wiki-v2:evidence-start -->
### 근거 ID
- `ref-011`
- `ref-013`
- `ref-014`
<!-- wiki-v2:evidence-end -->

- [[A Case against the GO TO Statement]]
- [[Structured Programming]]
- [[Structured Programming with go to Statements]]

## 관련 항목

- [[제어 흐름]] — 프로그램 명령들이 실행되는 순서와 분기·반복·호출·복귀의 흐름을 가리키는 개념.
- [[하드웨어 실행 모델]] — 연산(ALU)·저장(RAM)·제어(PC)의 3축 분리와 버스 분업 위에서 명령 사이클을 회전시키는 물리적 컴퓨터 동작 구조.
- [[GOTO 문]] — 프로그램 실행을 지정된 라벨이나 주소로 직접 이동시키는 제어문.
- [[구조적 프로그래밍]] — 프로그램을 임의 점프가 아니라 이해·검증 가능한 제어 구조와 데이터 구조로 작성하려는 방법론.
- [[단계적 정제]] — 추상적이고 이해 가능한 프로그램 구조에서 출발해 구현 세부로 내려가는 설계 방법.
