---
title: 메모리 장벽
aliases: [Memory wall, 메모리 월, processor-memory performance gap]
summary: "프로세서의 연산 속도와 메모리의 지연·대역폭 개선 사이의 격차 때문에 실제 실행 시간이 데이터 이동과 대기에 지배되는 성능 한계."
tags: [type/concept, domain/computer-architecture, domain/computer-science, status/active]
created: 2026-07-16
updated: 2026-07-25
sources: ["Slave Memories and Dynamic Storage Allocation", "Hitting the Memory Wall", "Roofline An Insightful Visual Performance Model", "The Landscape of Parallel Computing Research - A View from Berkeley", "FlashAttention - Fast and Memory-Efficient Exact Attention with IO-Awareness", "Efficient Memory Management for Large Language Model Serving with PagedAttention", "GPTQ - Accurate Post-Training Quantization for Generative Pre-trained Transformers", "AWQ - Activation-aware Weight Quantization for On-Device LLM Compression and Acceleration"]
status: active
graph_id: concept-df7e932b999f444f
event_start: 1995
historical_layer: architecture
historical_note: "프로세서와 메모리 성능 격차를 memory wall로 분석한 논문이 출판된 1995년을 대표 시점으로 둔다."
---

## 개요

[[메모리 장벽]](memory wall)은 프로세서가 명령과 산술을 처리하는 속도에 비해 주 메모리에서 데이터를 가져오는 지연 시간과 대역폭이 충분히 개선되지 않아, 전체 실행 시간이 데이터 이동과 대기에 지배되는 현상이다. Wulf와 McKee의 [[Hitting the Memory Wall]]이 1995년에 이 표현을 널리 알려진 문제로 정식화했다.

## 지연 시간과 대역폭

메모리 성능은 하나의 숫자가 아니다.

- **지연 시간(latency)**은 요청한 데이터가 도착할 때까지 기다리는 시간이다.
- **대역폭(bandwidth)**은 일정 시간에 옮길 수 있는 데이터의 양이다.
- **프로세서 사이클 기준 비용**은 같은 나노초의 지연도 더 빠른 프로세서에서는 더 많은 작업 기회를 잃게 함을 보여준다.

독립적인 메모리 요청을 겹치면 대역폭을 활용할 수 있지만, 다음 주소가 이전 결과에 의존하는 접근은 긴 지연 시간을 숨기기 어렵다. 따라서 같은 하드웨어에서도 순차 접근, 불규칙 접근과 포인터 추적은 서로 다른 병목을 보인다.

## 완화 방법과 한계

[[캐시 메모리]]는 자주 쓰는 데이터를 프로세서 가까이에 두고, 프리페칭은 필요한 데이터를 미리 가져온다. [[Slave Memories and Dynamic Storage Allocation]]이 보여주듯 캐시의 효과는 빠른 층의 존재만이 아니라 프로그램의 재참조 패턴과 사상·교체·쓰기 정책에 달려 있다. 데이터 배치와 블로킹은 지역성을 높이고, 여러 요청을 동시에 처리하는 메모리 수준 병렬성은 대기를 겹친다. 더 넓은 메모리 채널과 온칩 메모리도 대역폭을 늘릴 수 있다.

이 방법들은 장벽을 완화하지만 없애지는 않는다. 작업 집합이 캐시보다 크거나 접근 패턴이 불규칙하고, 공유 데이터의 일관성·동기화 비용이 크면 데이터 이동이 다시 성능을 제한한다. 또한 계층을 복잡하게 만들수록 소프트웨어가 지역성을 표현하고 활용하는 일이 중요해진다.

## Roofline과의 연결

[[Roofline An Insightful Visual Performance Model]]은 메모리 장벽을 연산 집약도와 대역폭으로 정량화한다.

`달성 성능 ≤ min(최고 연산 성능, 메모리 대역폭 × 연산 집약도)`

바이트당 연산 수가 낮은 작업은 연산기가 아무리 빨라도 메모리 대역폭 지붕에 제한된다. 이 모델은 최고 FLOP/s가 실제 성능을 대표하지 못하는 이유와, 데이터 재사용을 늘리는 최적화가 왜 중요한지를 보여준다.

[[FlashAttention - Fast and Memory-Efficient Exact Attention with IO-Awareness]]은 이 관점을 GPU의 HBM과 온칩 SRAM 사이로 옮긴다. 정확한 밀집 어텐션의 산술량을 없애지 않더라도 큰 중간 행렬을 HBM에 쓰고 읽는 일을 줄이면 벽시계 시간이 짧아질 수 있다. 이는 연산 수와 메모리 이동량을 별도 비용으로 기록해야 하는 사례다.

[[Efficient Memory Management for Large Language Model Serving with PagedAttention]]은 메모리 장벽의 또 다른 형태를 보여준다. 여기서는 한 어텐션 커널의 데이터 이동보다 여러 자동회귀 요청이 보존하는 [[KV 캐시]]의 용량·단편화가 활성 배치 크기를 제한한다. 실제 토큰 상태를 같은 GPU 메모리에 더 조밀하게 배치하면 연산기의 사양을 바꾸지 않고도 동일 지연 조건의 서비스 처리량이 높아질 수 있다.

[[GPTQ - Accurate Post-Training Quantization for Generative Pre-trained Transformers]]와 [[AWQ - Activation-aware Weight Quantization for On-Device LLM Compression and Acceleration]]의 [[LLM 가중치 양자화]]는 모델 가중치의 저장량과 HBM 읽기 바이트를 줄인다. 이 대응은 가중치 대역폭이 현재 병목이고 저비트 패킹·역양자화를 효율적으로 수행하는 커널이 있을 때 유리하다. 활성값·KV 상태나 계산 장치가 병목이면 비트 감소만으로 종단 속도를 보장하지 않는다.

## 컴퓨팅 능력에 주는 의미

메모리 장벽은 컴퓨팅 능력이 연산기의 속도만으로 결정되지 않음을 분명히 한다. 유효 성능을 비교하려면 작업의 데이터 크기와 접근 패턴, 캐시·메모리 구성, 달성 대역폭과 지연 시간을 함께 기록해야 한다. [[도메인 특화 가속기]]가 큰 온칩 메모리와 맞춤 데이터 경로를 사용하는 것도 연산보다 데이터 이동의 비용을 줄이기 위해서다.

## 관계

| 관계 | 대상 | 설명 | 근거 |
|---|---|---|---|
| broader | [[입출력 인지 어텐션]] | GPU 어텐션의 HBM–SRAM 이동 최적화는 연산기와 메모리 속도 격차에 대응하는 구체적 사례다. | [[FlashAttention - Fast and Memory-Efficient Exact Attention with IO-Awareness]] |
| broader | [[KV 캐시]] | LLM 서빙에서 캐시 용량과 단편화가 활성 배치와 처리량을 제한하는 구체적 메모리 병목을 포함한다. | [[Efficient Memory Management for Large Language Model Serving with PagedAttention]] |
| broader | [[LLM 가중치 양자화]] | 가중치 저장량과 HBM 전송을 줄여 LLM 생성의 메모리 용량·대역폭 제약에 대응하는 사례를 포함한다. | [[GPTQ - Accurate Post-Training Quantization for Generative Pre-trained Transformers]] |

## 출처

- [[Slave Memories and Dynamic Storage Allocation]]
- [[Hitting the Memory Wall]]
- [[Roofline An Insightful Visual Performance Model]]
- [[The Landscape of Parallel Computing Research - A View from Berkeley]]
- [[FlashAttention - Fast and Memory-Efficient Exact Attention with IO-Awareness]]
- [[Efficient Memory Management for Large Language Model Serving with PagedAttention]]
- [[GPTQ - Accurate Post-Training Quantization for Generative Pre-trained Transformers]]
- [[AWQ - Activation-aware Weight Quantization for On-Device LLM Compression and Acceleration]]

## 관련 항목

- [[컴퓨팅 능력이란 무엇인가]] — 컴퓨팅 능력을 계산 가능성, 복잡도, 프로그래밍 가능성, 실현 성능, 확장성, 자원 효율, 신뢰 가능한 결과의 일곱 층위로 구분하고 측정 조건을 정리한 개념.
- [[KV 캐시]] — 요청별 상태의 용량과 단편화가 LLM 활성 배치 수를 제한하는 사례를 제공한다.
- [[LLM 가중치 양자화]] — 가중치 용량과 데이터 이동을 줄이는 LLM 추론의 대응 방법이다.
- [[더 빠른 프로세서는 왜 더 빠른 프로그램을 보장하지 않는가]] — 프로세서의 클럭·최고 처리율이라는 잠재력이 작업 부하, 컴파일러, ISA, 마이크로아키텍처와 메모리 계층을 거쳐 실제 프로그램 성능으로 전환되는 조건을 분석한다.
- [[입출력 인지 어텐션]] — Transformer 어텐션을 GPU 메모리 계층에 맞춰 실행하는 방법을 정리한다.
