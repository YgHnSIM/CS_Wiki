---
title: 유니코드 정규화
aliases: [Unicode Normalization, NFC, NFD, NFKC, NFKD]
summary: "같은 문자열처럼 보이는 여러 코드 포인트 시퀀스를 목적에 맞는 동등성 기준으로 맞추는 절차."
tags: [type/concept, domain/computer-science, domain/text-processing, domain/security, status/active]
created: 2026-07-10
updated: 2026-07-25
sources: ["Unicode Normalization Forms", "Unicode Text Segmentation", "Unicode Security Mechanisms", CWE-20 Improper Input Validation]
status: active
graph_id: concept-9351a6279b74d274
---

## 개요

[[유니코드 정규화]]는 Unicode 문자열을 특정 동등성 기준에 맞는 표준 형태로 변환하는 절차다. 같은 글자가 하나의 precomposed code point로 표현될 수도 있고, 기본 문자와 결합 부호의 시퀀스로 표현될 수도 있기 때문에, 바이트 단위 비교만으로는 사용자가 같은 문자열이라고 느끼는 값을 놓칠 수 있다.

## 네 가지 형식

- NFC: canonical equivalence를 유지하면서 가능한 경우 합성형으로 만든다.
- NFD: canonical equivalence를 유지하면서 분해형으로 만든다.
- NFKC: compatibility equivalence까지 고려해 합성형으로 만든다.
- NFKD: compatibility equivalence까지 고려해 분해형으로 만든다.

NFC는 일반 텍스트 저장과 교환에서 자주 쓰이는 실용적 선택이다. NFKC와 NFKD는 호환 문자를 더 강하게 접기 때문에 검색, 식별자 비교, 보안 처리에서 유용할 수 있지만, 원래의 시각적·의미적 구분을 잃을 수 있다.

## 주의점

정규화는 보안의 전부가 아니다. 정규화 후에도 서로 다른 문자 체계의 [[동형이의 문자]]가 매우 비슷하게 보일 수 있다. 또한 정규화된 문자열 두 개를 이어 붙인 결과가 항상 같은 정규화 형식을 유지한다고 보장되지는 않으므로, 문자열 결합 후 다시 정규화가 필요할 수 있다.

## 입력 검증 경계

정규화는 표현을 바꾸는 정책이지, 값이 특정 사용자·경로·권한에 적합한지 확인하는 [[입력 검증]] 자체는 아니다. 식별자 정책이 특정 정규화 형식을 기준으로 한다면, 디코드 뒤 어느 시점에 정규화하고 그 결과를 검증·저장·비교·인가에 쓸지 일관되게 정해야 한다. 한 표현을 검사하고 다른 표현을 키나 권한 대상으로 쓰면 같은 글자처럼 보이는 값의 해석이 갈라질 수 있다.

반대로 NFKC·NFKD처럼 호환성 문자를 접는 방식은 의미·표시 차이를 잃을 수 있다. 따라서 “정규화했으니 안전하다”가 아니라, 어떤 동등성을 요구하는지, 원문 보존과 표시를 어떻게 할지, [[동형이의 문자]]·혼합 스크립트를 어떤 별도 정책으로 다룰지를 명시해야 한다.

## 관계

| 관계 | 대상 | 설명 | 근거 |
|---|---|---|---|
| synthesizes | [[입력 검증]] | 비교 정책의 정준 표현을 검증·저장·인가까지 일관되게 사용하되, 정규화가 검증·권한 확인을 대신하지 않도록 구분한다. | [[CWE-20 Improper Input Validation]], [[Unicode Security Mechanisms]] |

## 출처

- [[Unicode Normalization Forms]]
- [[Unicode Text Segmentation]]
- [[Unicode Security Mechanisms]]
- [[CWE-20 Improper Input Validation]]

## 관련 항목

- [[유니코드]] — 다양한 문자와 기호를 코드 포인트, 인코딩 형식, 정규화, 텍스트 경계 문제로 포괄하는 문자 체계.
- [[그래핌 클러스터]] — 사용자가 문자 하나로 인식하는 단위에 가까운 Unicode 텍스트 분할 단위.
- [[동형이의 문자]] — 서로 다른 코드 포인트가 시각적으로 비슷해 식별자와 보안 경계에서 혼동을 일으키는 문자 문제.
- [[인코딩 오류]] — 잘못된 바이트열, 부적절한 디코딩, 대체 문자 처리 때문에 생기는 텍스트 해석 오류.
- [[입력 검증]] — 정규화된 표현과 실제 소비 표현이 같은 계약을 따르게 한다.
